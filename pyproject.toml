[project]
name = "final-capstone-project-agentic-ai-smart-research-assistant"
version = "0.1.0"
description = "Locally-hosted IEEE research assistant using multi-agent AI and RAG"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
    "pydantic",
    "python-multipart",
    "aiofiles",
    "sentence-transformers",
    "chromadb",
    "transformers",
    "PyMuPDF",
    "requests"
]
